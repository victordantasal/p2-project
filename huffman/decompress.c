
/*
 * @params recebe um caminho válido de arquivo.huff
 * @output gera um arquivo descompactado como saída
 * @returns 1 se realizar todos os passo com sucesso
 * 			0 se houver algum erro.
 */
int huffman_decompress(char *file_path)
{
	//TODO compress method
	if(1)	//SUCCESSFUL
		return 1;
	else
		return 0;
}
